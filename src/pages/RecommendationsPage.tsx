import React from 'react';
import { AlertTriangle, Check, ChevronRight, Sparkles, X } from 'lucide-react';
import { useDashboard } from '../context/DashboardContext';

export const RecommendationsPage: React.FC = () => {
  const { recommendations, applyRecommendation, dismissRecommendation, applyAllHighPriority, showToast } = useDashboard();
  const active = recommendations.filter(item => !item.dismissed);
  return <div className="max-w-4xl mx-auto space-y-6">
    <header className="flex flex-col sm:flex-row sm:items-end justify-between gap-4"><div><p className="text-sm font-semibold text-blue-800">AI decision support</p><h1 className="text-3xl font-extrabold tracking-tight text-slate-950 mt-1">Prescriptive Recommendations</h1><p className="text-sm text-slate-500 mt-1">Actionable insights generated by analyzing your POS and inventory data.</p></div><button onClick={applyAllHighPriority} className="rounded-lg bg-blue-900 px-4 py-2.5 text-sm font-bold text-white">Apply all high priority</button></header>
    <div className="space-y-4">{active.map(rec => <article key={rec.id} className="bg-white rounded-xl border border-slate-200 shadow-sm p-5"><div className="flex items-start gap-4"><div className={`rounded-lg p-2 ${rec.priority === 'critical' ? 'bg-red-100 text-red-700' : 'bg-blue-100 text-blue-800'}`}><Sparkles className="w-5 h-5" /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold text-slate-950">{rec.title.split(':')[0]}</h2><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold uppercase text-slate-600">{rec.priority} priority</span></div><p className="text-sm text-slate-600 mt-2">Reason: {rec.description}</p><div className="flex gap-2 mt-4"><button onClick={() => applyRecommendation(rec.id)} className="inline-flex items-center gap-1 rounded-md bg-blue-900 px-3 py-2 text-xs font-bold text-white"><Check className="w-3.5 h-3.5" /> Apply</button><button onClick={() => dismissRecommendation(rec.id)} className="inline-flex items-center gap-1 rounded-md border border-slate-200 px-3 py-2 text-xs font-bold text-slate-600"><X className="w-3.5 h-3.5" /> Dismiss</button></div></div></div></article>)}</div>
    <section className="rounded-xl border border-blue-100 bg-blue-50/60 p-5 flex items-center justify-between gap-4"><div><h2 className="font-bold text-slate-950">Generate more insights</h2><p className="text-sm text-slate-600 mt-1">Run a deep scan on last month&apos;s data to find hidden optimization opportunities.</p></div><button onClick={() => showToast('Deep scan queued for the next ETL cycle', 'info')} className="whitespace-nowrap rounded-lg bg-blue-900 px-4 py-2 text-xs font-bold text-white">Scan now <ChevronRight className="inline w-3 h-3" /></button></section>
    {active.length === 0 && <div className="text-center text-sm text-slate-500"><AlertTriangle className="mx-auto mb-2 w-5 h-5" />No active recommendations.</div>}
  </div>;
};
